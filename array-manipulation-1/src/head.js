/* exported head */

/* function: return the first item in the input list
input: list (array)
output: first item in list
-Take the first item in the list
-Give back the first item of the list */

function head(array) {
  return array[0];
}
