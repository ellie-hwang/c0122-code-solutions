/* exported last */

/* function: To get the last item in a list
input: list (array)
output: last item
-Go to the last item of the list
-Give back the last item of the list */

function last(array) {
  return array[array.length - 1];
}
